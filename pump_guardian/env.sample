# Copy to `.env` next to app.py (filename `.env` is gitignored — never commit real keys)
MINIMAX_API_KEY=paste-your-key-here
# Optional
MINIMAX_API_BASE=https://api.minimax.io
MINIMAX_MODEL=MiniMax-M2.5
